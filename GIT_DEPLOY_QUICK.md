# ⚡ Быстрая шпаргалка: Деплой через Git

## 🎯 Первоначальная настройка (один раз)

### На сервере:

```bash
cd /root/cryptowatcher

# Инициализируйте Git
git init
git remote add origin <ваш-git-репозиторий>

# Синхронизируйте существующий код
git add .
git commit -m "Current server state"
git branch -M main
git pull origin main --allow-unrelated-histories

# Сделайте скрипт исполняемым
chmod +x deploy/deploy.sh
```

---

## 🔄 Обновление (каждый раз)

### На вашем компьютере:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

### На сервере:

```bash
cd /root/cryptowatcher
./deploy/deploy.sh
```

**Всё!** Скрипт сделает остальное автоматически.

---

## 🔙 Откат (если что-то пошло не так)

```bash
cd /root/cryptowatcher
git log --oneline -5  # посмотреть коммиты
git reset --hard HEAD~1  # откатиться на один коммит назад
cd frontend && npm run build && cd ..
sudo systemctl restart cryptowatcher
```

---

## ⚠️ Важно

- `.env` файл **НЕ** должен быть в Git (должен быть в `.gitignore`)
- Всегда проверяйте статус: `sudo systemctl status cryptowatcher`
- Тестируйте локально перед отправкой на сервер

