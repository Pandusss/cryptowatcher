# 🚀 Деплой обновлений через Git

## 📋 Ситуация
На сервере уже есть рабочая версия приложения, и вы хотите начать использовать Git для обновлений.

---

## 🔧 Первоначальная настройка (один раз)

### Шаг 1: Подготовка Git репозитория (на вашем компьютере)

Если у вас еще нет Git репозитория:

```bash
# На вашем компьютере
cd E:\tgBots\CryptoWatcher

# Инициализируйте Git (если еще не сделано)
git init

# Создайте .gitignore (если еще нет)
# Убедитесь, что в нем есть:
# .env
# venv/
# node_modules/
# frontend/dist/
# *.pyc
# __pycache__/
# *.log

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Создайте репозиторий на GitHub/GitLab и добавьте remote
git remote add origin https://github.com/yourusername/cryptowatcher.git
# или
git remote add origin git@github.com:yourusername/cryptowatcher.git

# Отправьте код
git push -u origin main
```

### Шаг 2: Настройка Git на сервере

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Установите Git (если еще не установлен)
sudo apt update
sudo apt install git -y

# Перейдите в директорию проекта
cd /root/cryptowatcher

# Инициализируйте Git репозиторий
git init

# Добавьте remote (замените на ваш репозиторий)
git remote add origin https://github.com/yourusername/cryptowatcher.git
# или для SSH
git remote add origin git@github.com:yourusername/cryptowatcher.git

# Настройте Git (если нужно)
git config user.name "Server"
git config user.email "server@example.com"
```

### Шаг 3: Синхронизация существующего кода с Git

**ВАЖНО:** Сначала нужно синхронизировать существующий код на сервере с Git репозиторием.

#### Вариант A: Код на сервере совпадает с Git (рекомендуется)

Если код на сервере идентичен тому, что в Git:

```bash
# На сервере
cd /root/cryptowatcher

# Добавьте все файлы в Git (кроме тех, что в .gitignore)
git add .

# Сделайте коммит текущего состояния
git commit -m "Current server state"

# Свяжите с удаленным репозиторием
git branch -M main
git fetch origin

# Синхронизируйте с удаленным репозиторием
# Если есть конфликты, разрешите их
git pull origin main --allow-unrelated-histories

# Или если хотите использовать код из Git (перезаписать локальный)
git reset --hard origin/main
# .env файл не будет затронут, так как он в .gitignore
```

#### Вариант B: Код на сервере отличается от Git

Если на сервере есть изменения, которых нет в Git:

```bash
# На сервере
cd /root/cryptowatcher

# Добавьте все в Git
git add .

# Сделайте коммит
git commit -m "Server state before sync"

# Получите код из Git
git fetch origin
git branch -M main

# Синхронизируйте (может потребоваться разрешить конфликты)
git pull origin main --allow-unrelated-histories

# .env файл не будет затронут, так как он в .gitignore
```

#### Вариант C: Использовать код из Git (перезаписать локальный)

Если хотите использовать только код из Git и игнорировать локальные изменения:

```bash
# На сервере
cd /root/cryptowatcher

# Получите код из Git
git fetch origin
git reset --hard origin/main
git clean -fd

# .env файл не будет затронут, так как он в .gitignore
```

---

## 🔄 Процесс обновления (каждый раз)

### На вашем компьютере:

```bash
# 1. Внесите изменения в код
# ... редактируете файлы ...

# 2. Проверьте, что все работает локально
# ... тестируете ...

# 3. Добавьте изменения в Git
cd E:\tgBots\CryptoWatcher
git add .

# 4. Сделайте коммит с понятным описанием
git commit -m "Описание изменений: что было добавлено/исправлено"

# 5. Отправьте на GitHub/GitLab
git push origin main
```

### На сервере:

#### Способ 1: Автоматический (рекомендуется)

Используйте скрипт `deploy/deploy.sh`:

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Перейдите в директорию проекта
cd /root/cryptowatcher

# Запустите скрипт деплоя
./deploy/deploy.sh
```

Скрипт автоматически:
- Получит обновления из Git
- Определит, что изменилось
- Пересоберет фронтенд (если нужно)
- Перезапустит бэкенд (если нужно)
- Проверит статус

#### Способ 2: Ручной

```bash
# Подключитесь к серверу
ssh root@your-server-ip

# Перейдите в директорию проекта
cd /root/cryptowatcher

# Получите обновления
git pull origin main

# Проверьте, что изменилось
git log HEAD~1..HEAD --oneline

# Если были изменения во фронтенде
cd frontend
npm install  # только если изменился package.json
npm run build
cd ..

# Если были изменения в бэкенде
sudo systemctl restart cryptowatcher

# Проверьте статус
sudo systemctl status cryptowatcher
```

---

## 🛡️ Защита важных файлов

### Файлы, которые НЕ должны попадать в Git:

Убедитесь, что в `.gitignore` есть:

```
# Environment
.env
.env.*
!.env.example

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Node
node_modules/
frontend/dist/
npm-debug.log*

# Database
*.db
*.sqlite

# Logs
*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
```

### Проверка на сервере:

```bash
# Проверьте, что .env не отслеживается Git
cd /root/cryptowatcher
git status

# Если .env показывается в статусе:
git rm --cached .env  # убрать из отслеживания, но не удалять файл
echo ".env" >> .gitignore
git add .gitignore
git commit -m "Add .env to gitignore"
```

---

## 🔙 Откат к предыдущей версии

Если что-то пошло не так:

```bash
# На сервере
cd /root/cryptowatcher

# Посмотреть историю коммитов
git log --oneline -10

# Откатиться к предыдущему коммиту
git reset --hard HEAD~1

# Или к конкретному коммиту
git reset --hard <commit-hash>

# Пересобрать фронтенд
cd frontend
npm run build
cd ..

# Перезапустить бэкенд
sudo systemctl restart cryptowatcher

# Проверить статус
sudo systemctl status cryptowatcher
```

---

## 📝 Скрипт для автоматического деплоя

Создайте файл `/root/cryptowatcher/deploy/deploy.sh`:

```bash
#!/bin/bash
set -e

PROJECT_DIR="/root/cryptowatcher"
cd "$PROJECT_DIR"

echo "🚀 Начинаем деплой..."

# Получаем обновления
echo "📥 Получаем обновления из Git..."
PREVIOUS_COMMIT=$(git rev-parse HEAD)
git pull origin main || {
    echo "❌ Ошибка при получении обновлений"
    exit 1
}
CURRENT_COMMIT=$(git rev-parse HEAD)

# Проверяем изменения
if [ "$PREVIOUS_COMMIT" = "$CURRENT_COMMIT" ]; then
    echo "ℹ️  Нет новых изменений"
    exit 0
fi

echo "✅ Получены изменения: $(git log -1 --oneline)"

# Проверяем изменения во фронтенде
if git diff "$PREVIOUS_COMMIT" "$CURRENT_COMMIT" --name-only | grep -q "^frontend/"; then
    echo "🔨 Обновляем фронтенд..."
    cd frontend
    
    # Обновляем зависимости если изменился package.json
    if git diff "$PREVIOUS_COMMIT" "$CURRENT_COMMIT" --name-only | grep -q "package.json"; then
        npm install
    fi
    
    npm run build
    cd ..
fi

# Проверяем изменения в бэкенде
if git diff "$PREVIOUS_COMMIT" "$CURRENT_COMMIT" --name-only | grep -q "^backend/"; then
    echo "🔄 Перезапускаем бэкенд..."
    sudo systemctl restart cryptowatcher
    sleep 2
    
    if sudo systemctl is-active --quiet cryptowatcher; then
        echo "✅ Бэкенд перезапущен"
    else
        echo "❌ Ошибка при перезапуске"
        sudo journalctl -u cryptowatcher -n 20 --no-pager
        exit 1
    fi
fi

echo "✅ Деплой завершен!"
```

Сделайте скрипт исполняемым:

```bash
chmod +x /root/cryptowatcher/deploy/deploy.sh
```

---

## ⚠️ Важные моменты

1. **`.env` файл** - никогда не должен попадать в Git. Он должен быть в `.gitignore`
2. **`venv/` и `node_modules/`** - не должны быть в Git
3. **Проверяйте статус** после каждого деплоя: `sudo systemctl status cryptowatcher`
4. **Делайте коммиты** с понятными сообщениями - поможет в отладке
5. **Тестируйте локально** перед отправкой на сервер

---

## 🚨 Решение проблем

### Проблема: "Your local changes would be overwritten by merge"

```bash
# Сохраните изменения
git stash

# Получите обновления
git pull origin main

# Восстановите изменения (если нужно)
git stash pop
```

### Проблема: Конфликты при merge

```bash
# Посмотрите конфликтующие файлы
git status

# Разрешите конфликты вручную, затем:
git add .
git commit -m "Resolve merge conflicts"
```

### Проблема: .env был случайно закоммичен

```bash
# Удалите из Git (но не с диска)
git rm --cached .env

# Добавьте в .gitignore
echo ".env" >> .gitignore
git add .gitignore
git commit -m "Remove .env from tracking"

# Если .env уже в удаленном репозитории, нужно удалить его и там тоже
git push origin main
```

---

## 📋 Чеклист для первого деплоя

- [ ] Git репозиторий создан и код загружен
- [ ] `.gitignore` настроен правильно
- [ ] `.env` файл НЕ в Git
- [ ] На сервере установлен Git
- [ ] На сервере инициализирован Git репозиторий
- [ ] На сервере добавлен remote origin
- [ ] Существующий код на сервере синхронизирован с Git
- [ ] Скрипт деплоя создан и исполняемый
- [ ] Тестовый деплой выполнен успешно

---

## 🎯 Быстрая команда для деплоя

После первоначальной настройки, для каждого обновления:

**На компьютере:**
```bash
git add . && git commit -m "описание" && git push origin main
```

**На сервере:**
```bash
cd /root/cryptowatcher && ./deploy/deploy.sh
```

Всё! 🎉

