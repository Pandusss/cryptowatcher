# Настройка Redis

## Что такое Redis и зачем он нужен?

**Redis** (Remote Dictionary Server) — это быстрая база данных в памяти, которая используется для:
- **Кэширования** — хранение часто запрашиваемых данных для ускорения работы
- **Очередей задач** — для фоновых задач и уведомлений
- **Временного хранения** — данные хранятся в оперативной памяти, очень быстро

В нашем проекте Redis используется для **кэширования данных** от CoinGecko API, чтобы:
- Уменьшить количество запросов к API (экономия лимитов)
- Ускорить ответы приложения (в 100-2000 раз быстрее)
- Снизить нагрузку на внешний API
- Улучшить производительность проверки уведомлений

**Важно:** Redis **опционален** — приложение будет работать и без него, просто без кэширования.

---

## 📋 Содержание

1. [Установка Redis на Windows (для разработки)](#установка-redis-на-windows-для-разработки)
2. [Установка Redis на Linux (для продакшена)](#установка-redis-на-linux-для-продакшена)
3. [Настройка в проекте](#настройка-в-проекте)
4. [Проверка работы](#проверка-работы)
5. [Управление Redis](#управление-redis)

---

## Установка Redis на Windows (для разработки)

### Вариант 1: Через WSL (Windows Subsystem for Linux) — Рекомендуется

1. **Установите WSL** (если еще не установлен):
   ```powershell
   wsl --install
   ```
   Перезагрузите компьютер после установки.

2. **В WSL установите Redis**:
   ```bash
   sudo apt update
   sudo apt install redis-server
   ```

3. **Запустите Redis**:
   ```bash
   sudo service redis-server start
   ```

4. **Проверьте, что Redis работает**:
   ```bash
   redis-cli ping
   ```
   Должен ответить: `PONG`

### Вариант 2: Через Docker (если установлен Docker Desktop)

1. **Запустите Redis в контейнере**:
   ```powershell
   docker run -d -p 6379:6379 --name redis redis:latest
   ```

2. **Проверьте, что Redis работает**:
   ```powershell
   docker ps
   ```
   Должен быть запущен контейнер `redis`.

### Вариант 3: Через Memurai (Windows-версия Redis)

1. **Скачайте Memurai**:
   - Перейдите на https://www.memurai.com/
   - Скачайте бесплатную версию для разработки

2. **Установите Memurai**:
   - Запустите установщик
   - Следуйте инструкциям

3. **Memurai запустится автоматически** как служба Windows

---

## Настройка в проекте

### 1. Создайте `.env` файл (если еще не создан)

Скопируйте `env.example.txt` в `.env`:
```bash
cd backend
cp env.example.txt .env
```

### 2. Настройте REDIS_URL в `.env`

**Для Windows (локальная разработка):**
```env
REDIS_URL=redis://localhost:6379/0
```

**Для Linux сервера (продакшен):**
```env
# Если Redis на том же сервере без пароля:
REDIS_URL=redis://localhost:6379/0

# Если Redis на другом сервере:
REDIS_URL=redis://IP_АДРЕС_СЕРВЕРА:6379/0

# Если Redis с паролем:
REDIS_URL=redis://:ПАРОЛЬ@localhost:6379/0
```

**Формат URL:**
```
redis://[username]:[password]@[host]:[port]/[database]
```

Примеры:
- `redis://localhost:6379/0` - локально, без пароля
- `redis://:mypassword@localhost:6379/0` - локально, с паролем
- `redis://192.168.1.100:6379/0` - удаленный сервер

### 3. Запустите backend

**На Windows:**
```powershell
cd backend
python run.py
```

**На Linux:**
```bash
cd backend
python3 run.py
# или если используете venv:
source venv/bin/activate
python run.py
```

**Проверка подключения:**

Если Redis подключен успешно, в логах вы увидите:
```
[get_crypto_list] Данные из кэша Redis
```

Если Redis недоступен, вы увидите:
```
⚠️  Redis недоступен: ...
ℹ️  Продолжаем работу без Redis кэширования
```

Приложение продолжит работать без кэширования, но будет медленнее.

---

## Проверка работы Redis

### Проверка через командную строку:

**Если Redis установлен через WSL:**
```bash
wsl redis-cli ping
```

**Если Redis установлен через Docker:**
```powershell
docker exec -it redis redis-cli ping
```

**Если установлен Memurai:**
```powershell
redis-cli ping
```

Должен ответить: `PONG`

### Проверка через Python:

```python
import redis
r = redis.Redis(host='localhost', port=6379, db=0)
r.ping()  # Должен вернуть True
```

---

## Управление Redis

### Запуск Redis:

**WSL:**
```bash
sudo service redis-server start
```

**Docker:**
```powershell
docker start redis
```

**Memurai:**
- Запускается автоматически как служба Windows
- Или через "Службы" Windows

### Остановка Redis:

**WSL:**
```bash
sudo service redis-server stop
```

**Docker:**
```powershell
docker stop redis
```

**Memurai:**
- Через "Службы" Windows
- Или через командную строку: `net stop memurai`

### Просмотр данных в Redis:

```bash
redis-cli
```

В консоли Redis:
```redis
KEYS *  # Показать все ключи
GET coin_details:bitcoin  # Получить значение по ключу
FLUSHALL  # Очистить все данные (осторожно!)
```

---

## Работа без Redis

Если вы не хотите использовать Redis, приложение будет работать без него:
- Все запросы будут идти напрямую к CoinGecko API
- Кэширование не будет работать
- Это нормально для разработки, но **не рекомендуется для продакшена**

Просто убедитесь, что в `.env` указан правильный `REDIS_URL`, даже если Redis не запущен — приложение автоматически определит, что Redis недоступен, и продолжит работу.

---

## 🔧 Настройка для продакшена (Linux)

### Автозапуск Redis при перезагрузке сервера:

Redis уже настроен на автозапуск после установки через `systemctl enable`. Проверьте:

```bash
sudo systemctl is-enabled redis-server
# или для CentOS:
sudo systemctl is-enabled redis
```

Должно вернуть: `enabled`

### Настройка firewall (если Redis на отдельном сервере):

Если Redis находится на отдельном сервере, откройте порт:

**UFW (Ubuntu):**
```bash
sudo ufw allow 6379/tcp
```

**firewalld (CentOS):**
```bash
sudo firewall-cmd --permanent --add-port=6379/tcp
sudo firewall-cmd --reload
```

### Мониторинг Redis:

```bash
# Проверить статус
sudo systemctl status redis-server

# Посмотреть логи
sudo journalctl -u redis-server -f

# Проверить использование памяти
redis-cli info memory
```

---

## Устранение проблем

### Ошибка "Connection refused"

- Убедитесь, что Redis запущен
- Проверьте порт в `REDIS_URL` (по умолчанию 6379)
- Проверьте, не блокирует ли файрвол подключение

### Ошибка "Redis недоступен" в логах

- Это нормально, если Redis не установлен или не запущен
- Приложение продолжит работать без кэширования

### Redis не запускается

- Проверьте логи Redis
- Убедитесь, что порт 6379 не занят другим приложением
- Попробуйте переустановить Redis

